<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<data>
      
		<record id="financial_statement_heading_line_view_form" model="ir.ui.view">
			<field name="name">Rubro de Estado Financiero</field>
            <field name="model">financial.statement.heading.line</field>
            <field name="arch" type="xml">
            	<form>
                    <sheet>
                        <group>
            		      <group>
                                <field name="nro_rubro"/>
                                <field name="name"/>
                                <field name="is_total"/>
                                <field name="is_title"/>
                                <field name="title" invisible="is_title == False"/>
                                <field name="code_sunat"/>
                                <field name="group_heading_financial_statement_id"/>
                         </group>
                        </group>
                        
                        <br/>

                        <group>
                            <group string="Cálculo de Movimientos">

                                <field name="calculation_type"/>

                                <field name="movements_period" invisible="calculation_type != 'manual'"/>
                                <field name="account_ids" invisible="calculation_type != 'accounts'" widget="many2many_tags" options="{'no_create': True}"/>
                            </group>
                        </group>
                    </sheet>
                </form>            	
            </field>
		</record>
		
	</data>
</odoo>
